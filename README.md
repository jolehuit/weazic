AI-based website that uses information such as the user's local weather (retrieved via the OpenWeather API), mood and favorite music styles to generate relevant Spotify playlists that they are likely to enjoy.
